{
  "name": "ai-translator-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "cd front && npm install && npm run build && cd .. && cp -r front/dist public && rm -rf front/node_modules"
  },
  "dependencies": {
    "@vercel/blob": "^0.22.1"
  }
}
