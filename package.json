{
  "name": "ai-translator-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd front && npm install && npm run build && cp -r dist ../public"
  },
  "dependencies": {
    "@vercel/blob": "^0.22.1"
  }
}
